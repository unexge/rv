//! File-wide line builder for modified files. Stitches the SST diff into
//! a flat `[]StyledLine` keyed off the underlying source lines, then runs
//! `elide.zig` to collapse long unchanged runs.
//!
//! Stub: real implementation lands in subtask 3.

pub fn build() !void {
    return error.NotImplemented;
}
